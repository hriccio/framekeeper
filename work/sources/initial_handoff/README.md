# Initial Handoff Sources

This folder preserves the source context supplied when `framekeeper` was
created.

Files:

- `um_outro_henrique-content_automation_system-context.md`: video processing
  and content automation system context.
- `github-pages-knowledge-layer-codex-handoff.md`: public knowledge-layer
  context for deriving structured markdown/site content from videos.

Treat these as evidence for extraction. Do not edit them to match later model
decisions; create refined artifacts under `docs/semantics/`, `docs/slices/`,
and `work/changes/` instead.
